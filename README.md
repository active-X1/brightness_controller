# brightness_controller
A lightweight Python desktop app for controlling screen brightness with a clean CustomTkinter interface.  Uses customtkinter for modern theming and UI. Uses screen_brightness_control to read and set monitor brightness. Includes a slider, current brightness display, theme toggle, and an About dialog.
